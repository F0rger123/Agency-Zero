# Agency Zero — Decision Log

Records **why** things are the way they are. MASTER_SPEC.md records **what** is required.
Add a new row for every meaningful architectural, design, or scope decision. Never delete rows — supersede them if a decision changes.

| ID | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-001 | 2026-09-17 | Documentation-first: create MASTER_SPEC, DECISIONS, IDEAS_BACKLOG, BUILD_PROGRESS, DATABASE_PLAN before writing any application code. | Future AI agents must never lose the original requirements; the spec is the source of truth. | Active |
| D-002 | 2026-09-17 | MASTER_SPEC.md is the single source of truth; it is additive-only. Requirements are never removed unless the user explicitly removes them; new ideas are merged into existing sections. | Prevents silent requirement loss across long-running AI-assisted development. | Active |
| D-003 | 2026-09-17 | App is a private tool for a single agency owner (the user). Not a multi-tenant SaaS. | The product is the user's personal agency operating system; multi-tenancy would add complexity with no current need. | Active |
| D-004 | 2026-09-17 | Tech stack: Next.js + TypeScript + Tailwind + Supabase (Postgres, Auth, Storage). | User-specified direction. Supabase gives database, auth, and file storage in one service, keeping the stack lean. | Active |
| D-005 | 2026-09-17 | UI system: black / white / grayscale only, minimal borders and cards, generous spacing, clean typography, no gradients, no accent colors. Desktop-first, mobile usable. Speed and usability over flash. | User-specified. A monochrome system keeps the app calm, fast to design, and consistent; states are conveyed with labels/contrast rather than color. | Active |
| D-006 | 2026-09-17 | Build in phases (see BUILD_PROGRESS.md). Core CRM first (clients → projects → tasks → schedule), sales documents next, AI assistant after data exists, portal and external integrations last. | The AI and integrations depend on data models that don't exist yet; core CRUD delivers daily value first. | Active |
| D-007 | 2026-09-17 | Integrations (Stripe, Google Calendar, Meta Ads, Search Console, GA4) are deferred. Architecture only prepares for them via adapter/provider boundaries and integration-ready schema. | User instruction: do not attempt to build every integration now. Avoid premature API work while keeping future cost low. | Active |
| D-008 | 2026-09-17 | AI safety boundary: the AI assistant may read data and propose actions, but must never silently perform high-impact financial or ad-spend actions. Future ad automation goes through an approval flow; eventual automation is optional and guarded (bounded, pre-approved rules). | Money and ad spend are irreversible/high-risk; the owner must always approve. Hard rule, restated in MASTER_SPEC §4.10/§4.12. | Active |
| D-009 | 2026-09-17 | Payments are recorded manually at first (deposits, partials, external payments); Stripe comes later. | Invoice/balance logic is needed regardless of payment rail; manual recording is fully functional on day one. | Active |
| D-010 | 2026-09-17 | Client-facing surfaces (quote view, contract view/signing, later the Client Portal) use public secure tokenized links rather than client accounts, until the portal phase. | Lowest-friction acceptance for clients; no auth to manage. Portal phase may add authenticated client access. | Active |
| D-011 | 2026-09-17 | Electronic contract signing stores a signed timestamp and the signed document (PDF snapshot) with version history; full cryptographic e-signature compliance (e.g. ESIGN/UETA audit trails) is an enhancement to revisit at build time. | The requirement is secure links + stored proof of signing; start simple, upgrade if legal robustness is needed. | Active |
| D-012 | 2026-09-17 | Money stored as integer minor units (cents) with a currency field; all timestamps stored in UTC; time tracking convention (hours vs minutes) to be fixed at build time and recorded here. | Prevents floating-point money bugs and timezone drift; consistency must be decided once, not per-feature. | Active — duration convention resolved by D-015 |
| D-013 | 2026-09-17 | Database design lives in DATABASE_PLAN.md as a living preliminary plan; migrations in the app are the eventual source of truth once Phase 1 starts. | Lets schema thinking evolve in docs first, then be locked in by migrations. | Active — migrations 0001–0004 now exist; DATABASE_PLAN.md kept in sync via its migration log |
| D-014 | 2026-09-17 | RLS policy pattern (Phase 1): every table gets RLS enabled; policies are `to authenticated` only. Internal tables: full access for any authenticated user (the owner — this is a single-owner private app, D-003). `profiles` is scoped to `auth.uid() = id`. Future public token surfaces (quote/contract links, portal) get their own narrow, scoped policies. | Simple, secure default for a private app; avoids premature per-client policy complexity while making later narrowing straightforward. | Active |
| D-015 | 2026-09-17 | Durations are stored in **minutes** everywhere (DB columns `*_minutes`, e.g. `estimated_minutes`, `actual_minutes`, `default_daily_capacity_minutes`); the UI renders hours. | Settles the open convention in D-012; minutes avoid decimal-hour rounding bugs and match calendar math. | Active |
| D-016 | 2026-09-17 | Design system implementation: Tailwind v4 CSS `@theme` tokens (background, foreground, muted, border, muted-foreground, faint-foreground, inverted, inverted-foreground) — grayscale only; light theme only for Phase 1 (no dark mode); system font stack (no webfont download); hairline dividers and spacing instead of cards; icons are inline monochrome line SVGs. | Follows MASTER_SPEC §3 exactly and keeps the app fast (no font fetch, tiny CSS). Dark mode can be added later as a token swap if ever wanted. | Active |
| D-017 | 2026-09-17 | Phase 1 pulled the clients, projects, and tasks **schema** forward into the initial migrations (owner instruction), while their CRUD interfaces stay in Phases 2–3. `projects.currency` added per D-012; `tasks.milestone_id`, `task_dependencies`, `recurring_tasks`, and `time_entries` deferred to Phase 3 migrations with their features. | Schema-first gives every later phase a stable foundation; features still ship in roadmap order. | Active |
| D-018 | 2026-09-17 | No fake UI: pages for unbuilt features show honest "Phase N — planned" empty states derived from the roadmap; dashboard numbers are real database counts; infrastructure problems get explicit setup states (missing env, unapplied migrations) instead of errors or mock data. | Owner requirement: no placeholder buttons that pretend to work. Keeps trust in the product and makes phase boundaries visible. | Active |
| D-019 | 2026-09-17 | Auth plumbing: Next.js 16 `proxy.ts` (middleware successor) refreshes Supabase sessions and guards all routes except `/login` and `/auth/callback`; layouts and protected pages double-check `getUser()` server-side; owner account is created in the Supabase dashboard (no sign-up route). | Defense in depth for a private app; Next 16 convention keeps the app future-proof. | Active |
