# Agency Zero — Decision Log

Records **why** things are the way they are. MASTER_SPEC.md records **what** is required.
Add a new row for every meaningful architectural, design, or scope decision. Never delete rows — supersede them if a decision changes.

| ID | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-001 | 2026-09-17 | Documentation-first: create MASTER_SPEC, DECISIONS, IDEAS_BACKLOG, BUILD_PROGRESS, DATABASE_PLAN before writing any application code. | Future AI agents must never lose the original requirements; the spec is the source of truth. | Active |
| D-002 | 2026-09-17 | MASTER_SPEC.md is the single source of truth; it is additive-only. Requirements are never removed unless the owner explicitly removes them; new ideas are merged into existing sections. | Prevents silent requirement loss across long-running AI-assisted development. | Active |
| D-003 | 2026-09-17 | App is a private tool for a single agency owner (the user). Not a multi-tenant SaaS. | The product is the user's personal agency operating system; multi-tenancy would add complexity with no current need. | Active |
| D-004 | 2026-09-17 | Tech stack: Next.js + TypeScript + Tailwind + Supabase (Postgres, Auth, Storage). | User-specified direction. Supabase gives database, auth, and file storage in one service, keeping the stack lean. | Active |
| D-005 | 2026-09-17 | UI system: black / white / grayscale only, minimal borders and cards, generous spacing, clean typography, no gradients, no accent colors. Desktop-first, mobile usable. Speed and usability over flash. | User-specified. A monochrome system keeps the app calm, fast to design, and consistent; states are conveyed with labels/contrast rather than color. | Active |
| D-006 | 2026-09-17 | Build in phases (see BUILD_PROGRESS.md). Core CRM first (clients → projects → tasks → schedule), sales documents next, AI assistant after data exists, portal and external integrations last. | The AI and integrations depend on data models that don't exist yet; core CRUD delivers daily value first. | Active |
| D-007 | 2026-09-17 | Integrations (Stripe, Google Calendar, Meta Ads, Search Console, GA4) are deferred. Architecture only prepares for them via adapter/provider boundaries and integration-ready schema. | User instruction: do not attempt to build every integration now. Avoid premature API work while keeping future cost low. | Active |
| D-008 | 2026-09-17 | AI safety boundary: the AI assistant may read data and propose actions, but must never silently perform high-impact financial or ad-spend actions. Future ad automation goes through an approval flow; eventual automation is optional and guarded (bounded, pre-approved rules). | Money and ad spend are irreversible/high-risk; the owner must always approve. Hard rule, restated in MASTER_SPEC §4.10/§4.12. | Active |
| D-009 | 2026-09-17 | Payments are recorded manually at first (deposits, partials, external payments); Stripe comes later. | Invoice/balance logic is needed regardless of payment rail; manual recording is fully functional on day one. | Active |
| D-010 | 2026-09-17 | Client-facing surfaces (quote view, contract view/signing, later the Client Portal) use public secure tokenized links rather than client accounts, until the portal phase. | Lowest-friction acceptance for clients; no auth to manage. Portal phase may add authenticated client access. | Active |
| D-011 | 2026-09-17 | Electronic contract signing stores a signed timestamp and the signed document (PDF snapshot) with version history; full cryptographic e-signature compliance (e.g. ESIGN/UETA audit trails) is an enhancement to revisit at build time. | The requirement is secure links + stored proof of signing; start simple, upgrade if legal robustness is needed. | Active |
| D-012 | 2026-09-17 | Money stored as integer minor units (cents) with a currency field; all timestamps stored in UTC; time tracking convention (hours vs minutes) to be fixed at build time and recorded here. | Prevents floating-point money bugs and timezone drift; consistency must be decided once, not per-feature. | Active — duration convention resolved by D-015 |
| D-013 | 2026-09-17 | Database design lives in DATABASE_PLAN.md as a living preliminary plan; migrations in the app are the eventual source of truth once Phase 1 starts. | Lets schema thinking evolve in docs first, then be locked in by migrations. | Active — migrations 0001–0007 now exist; DATABASE_PLAN.md kept in sync via its migration log |
| D-014 | 2026-09-17 | RLS policy pattern (Phase 1): every table gets RLS enabled; policies are `to authenticated` only. Internal tables: full access for any authenticated user (the owner — this is a single-owner private app, D-003). `profiles` is scoped to `auth.uid() = id`. Future public token surfaces (quote/contract links, portal) get their own narrow, scoped policies. | Simple, secure default for a private app; avoids premature per-client policy complexity while making later narrowing straightforward. | Active |
| D-015 | 2026-09-17 | Durations are stored in **minutes** everywhere (DB columns `*_minutes`, e.g. `estimated_minutes`, `actual_minutes`, `default_daily_capacity_minutes`); the UI renders hours. | Settles the open convention in D-012; minutes avoid decimal-hour rounding bugs and match calendar math. | Active |
| D-016 | 2026-09-17 | Design system implementation: Tailwind v4 CSS `@theme` tokens (background, foreground, muted, border, muted-foreground, faint-foreground, inverted, inverted-foreground) — grayscale only; light theme only for Phase 1 (no dark mode); system font stack (no webfont download); hairline dividers and spacing instead of cards; icons are inline monochrome line SVGs. | Follows MASTER_SPEC §3 exactly and keeps the app fast (no font fetch, tiny CSS). Dark mode can be added later as a token swap if ever wanted. | Active |
| D-017 | 2026-09-17 | Phase 1 pulled the clients, projects, and tasks **schema** forward into the initial migrations (owner instruction), while their CRUD interfaces stay in Phases 2–3. `projects.currency` added per D-012; `tasks.milestone_id`, `task_dependencies`, `recurring_tasks`, and `time_entries` were initially deferred to the Phase 3 migration with their features. | Schema-first gives every later phase a stable foundation; features still ship in roadmap order. | Superseded by D-020 for the delivered Phase 2–3 schema |
| D-018 | 2026-09-17 | No fake UI: pages for unbuilt features show honest "Phase N — planned" empty states derived from the roadmap; dashboard numbers are real database counts; infrastructure problems get explicit setup states (missing env, unapplied migrations) instead of errors or mock data. | Owner requirement: no placeholder buttons that pretend to work. Keeps trust in the product and makes phase boundaries visible. | Active |
| D-019 | 2026-09-17 | Auth plumbing: Next.js 16 `proxy.ts` (middleware successor) refreshes Supabase sessions and guards all routes except `/login`, `/auth/callback`, `/q/*`, and `/c/*`; layouts and protected pages double-check `getUser()` server-side; owner account is created in the Supabase dashboard (no sign-up route). | Defense in depth for a private app; Next 16 convention keeps the app future-proof while allowing only tokenized customer surfaces to remain public. | Active |
| D-020 | 2026-09-17 | Phases 2–3 CRM schema is delivered in one additive migration, `0005_crm_core.sql`, so client, project, and task detail surfaces can be used together. The migration includes the previously deferred milestones, many-to-many dependency table, recurring-task template table, and time entries; the existing simple task dependency field remains for fast everyday entry. | The user requested the real CRM core as a connected unit. A single ordered migration keeps foreign keys and RLS consistent while preserving the original Phase 2/3 entity plan. | Active |
| D-021 | 2026-09-17 | Client files use a private `client-files` Supabase Storage bucket. Uploads are server actions with owner auth, a 10 MB size guard, metadata rows, and one-hour signed download URLs; the UI reports a setup error if the bucket/migration is unavailable. | Files contain client information and must not be public. Server-side uploads and expiring links keep the initial implementation lightweight without inventing an integration. | Active |
| D-022 | 2026-09-17 | CRUD forms use server actions with both browser constraints and server-side validation. Money is entered as currency units and stored as integer cents; durations are entered as hours and stored as integer minutes. | Keeps the UI natural for an agency owner while enforcing D-012 and D-015 at the database boundary. | Active |
| D-023 | 2026-09-17 | Public quote and contract links use random 256-bit URL tokens plus SHA-256 lookup hashes. Anonymous customers interact through narrowly scoped security-definer RPCs rather than broad anonymous table policies. | Public table reads would expose every tokenized record to anonymous clients. Hash lookup and purpose-built functions keep the link surface narrow without requiring customer accounts. | Active |
| D-024 | 2026-09-17 | Scheduled workload is the sum of open tasks with a planned date and estimated minutes, plus unlinked calendar work blocks. Work blocks linked to tasks are not double-counted. | This gives the owner one clear available-vs-scheduled calculation while allowing calendar blocks to represent either a task or independent planned work. | Active |
| D-025 | 2026-09-17 | Invoice paid/balance/status values are derived from manual payment rows by a Postgres trigger; Stripe is not connected. Overdue status is refreshed when invoice/reminder pages load. | Payment history remains the source of truth, prevents inconsistent cached balances, and keeps the manual-first sales phase fully functional. | Active |
| D-026 | 2026-09-17 | The automatic client-no-response reminder is calculated when the latest communication for an active/lead client is outgoing and at least three UTC days old, with no newer incoming communication in the loaded history window. | This makes the existing MASTER_SPEC trigger actionable without inventing a mail-sync integration; the threshold is visible in code and can be made configurable in a later reminders phase. | Active |
| D-027 | 2026-09-17 | Cloudflare deployment uses the manually configured `@opennextjs/cloudflare` adapter and Wrangler rather than Cloudflare's automatic framework setup. The existing Next.js 16 `src/proxy.ts` remains the auth boundary; every adapter or Next.js upgrade is gated by an OpenNext build and Workers preview because Node.js middleware support is experimental. | This keeps the current Next.js app deployable without switching frameworks or adding product features, while making the known runtime compatibility boundary explicit. | Active |
| D-028 | 2026-09-18 | Quote options model (migration `0008`): every line item has `selection` — `fixed` (always included), `optional` (customer checkbox add-on), or `choice` (mutually exclusive pick-one within the same `option_group`, i.e. packages). Header totals assume every option is taken; on acceptance the new `respond_public_quote` RPC validates the selection server-side and stores a frozen snapshot (`selected_item_ids`, `accepted_subtotal_cents`, `accepted_total_cents`, `responded_by`). The stored quote discount is capped at the accepted subtotal when totals are recomputed. | One column cleanly covers both "optional items" and "packages/options" without extra tables; server-side validation and snapshots keep the public acceptance surface honest and auditable while staying backwards compatible (legacy rows read as `fixed`). | Active |
| D-029 | 2026-09-18 | Immutability is enforced by database triggers, not app code: once a quote is `accepted` or a contract is `signed`, the row's business columns, its line items/version history, and deletes are frozen for every role (including the owner). Only bookkeeping columns stay writable: `updated_at`, token rotation (`public_token(_hash)`, `token_expires_at`), `converted_project_id`, and quote/project association links. | What the customer accepted or signed is a permanent record; app-level checks alone can be bypassed from any client. Whitelisting bookkeeping columns keeps link rotation and quote→project conversion working without weakening the legal record. | Active |
| D-030 | 2026-09-18 | `0008_security_hardening.sql` also closes Supabase security-lint findings without touching `0001`–`0007`: `public.set_updated_at()` is pinned to `search_path = public`, and EXECUTE is revoked from `public`, `anon`, and `authenticated` on the trigger-only functions `handle_new_user()` and `recalculate_invoice_payment()` (trigger invocation bypasses the EXECUTE check, so behaviour is unchanged). New trigger functions get the same revocation treatment. | Trigger functions with an unset search_path or public EXECUTE are classic privilege-escalation surfaces; pinning and revoking is the documented Supabase hardening pattern, delivered additively in one migration. | Active |
| D-031 | 2026-09-18 | Faster navigation is achieved with prefetching plus a bounded router cache: the sidebar prefetches nav routes on hover/focus and warms every route shortly after mount; `experimental.staleTimes.dynamic = 30` keeps prefetched dynamic pages instant for 30 seconds. Mutations remain exact because every server action calls `revalidatePath`. | All app pages are `force-dynamic`, so default prefetching does nothing; a 30-second staleness window is an acceptable trade against permanently slow sidebar navigation, and revalidation keeps post-mutation screens current. | Active |
| D-032 | 2026-09-18 | The wordmark's glitch effect is pure CSS: two grayscale shadow copies of the text (`::before`/`::after` with `content: attr(data-text)`) stay invisible for ~91% of a 7s loop and briefly tear via `clip-path` slices and 1–2px offsets. No JavaScript, no hue, no gradient; disabled entirely under `prefers-reduced-motion`. Settings are editable through validated server actions against the existing `profiles`/`settings` rows (no new tables). | Monochrome rule §3 keeps the effect tasteful and cheap; CSS-only animation costs no runtime in the app shell; settings persistence needed only server-side validation since RLS policies already scoped the rows to the owner. | Active |
